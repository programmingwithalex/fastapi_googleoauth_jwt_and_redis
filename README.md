# fastapi_googleoauth_jwt_and_redis
Example of how to deploy a `Flask` frontend with a `FastAPI` auth service using `Google OAuth2`, JWT (access and refresh tokens), and `Redis` (refresh token storage and invalidation)
